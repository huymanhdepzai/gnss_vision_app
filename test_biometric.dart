import 'package:local_auth/local_auth.dart';

void main() {
  final auth = LocalAuthentication();
  print('Methods of LocalAuthentication:');
  // This won't work to print methods at runtime easily without reflection, 
  // but we can try to compile a small snippet to see what's accepted.
}
